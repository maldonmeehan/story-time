<%= render "layouts/errors", :object => @story %>

<%= form_for @story do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.hidden_field :image, :value => @random %>
  <%= f.submit  %>
<% end %>
